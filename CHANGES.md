0.3.0

Added `Generator.recursive().withBase(...)` and internal depth checking to prevent endless recursion.

Added `GenerationEnvironment.generative` to allow generating from external int streams, e.g., for a coverage-guided fuzzers
    (by Vladimir Sitnikov)

0.2.3

No release notes before this point